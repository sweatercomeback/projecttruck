<h1><%= @vehicle.title %> </h1>
<div id="info">
<h2><%= @vehicle.year %> <%= @vehicle.model.make.name %> <%= @vehicle.model.name %> <%= @vehicle.model_other %></h2>
<p><strong>Condition:</strong> <%= @vehicle.vehicle_condition.name %> <%= @vehicle.vehicle_condition_comments %></p>
<p><strong>Mileage:</strong> <%= @vehicle.mileage %></p>
<p><strong>Engine:</strong> <%= @vehicle.engine.name %> <%= @vehicle.engine_comments %></p>
<p><strong>Drive:</strong> <%= @vehicle.drive.name %> <%= @vehicle.drive_comments %></p>
<p><strong>Transmission:</strong> <%= @vehicle.transmission.name %> <%= @vehicle.transmission_comments %></p>
<p><strong>Number of Doors:</strong> <%= @vehicle.doors %></p>
<p><strong>Exterior Color:</strong> <%= @vehicle.ext_color_comments %></p>
<p><strong>Interior Color:</strong> <%= @vehicle.int_color_comments %></p>
<p><%= @vehicle.additional_comments %></p>
<br />
<div id="projects">
<h3>Projects</h3>
    <% if @projects.blank? %>
    <p>No projects.</p>
    <% else %>
        <% @projects.each do |p| %>
        <p><strong><%= h p.title %></strong></p>
        <% end %>
    <% end %>
</div>
</div>




<div id="checkList">
<ul>
	<li><%= bool_image(@vehicle.for_sale) %>For Sale <% if @vehicle.for_sale %>($<%= @vehicle.price %>)<% end%> </li>
	<li><%= bool_image(@vehicle.street_legal) %>Street Legal </li>
</ul>
</div>

<div id="checkList">
<ul>
	<li><%= link_to "Edit Photos", {:controller => 'vehicle_photo', :action => "index", :vehicle_id => @vehicle.id }, :popup => ['vehicle_photos', 'height=500,width=600'] %></li>
	<li><%= link_to 'Back to Garage', {:controller => 'user', :action => 'home'} %></li>
	<li><%= link_to 'Edit', {:action => "edit", :id => @vehicle.id} %></li>
	<li><%= link_to 'Delete', {:action => "delete", :id => @vehicle.id}, :confirm => "Are you sure you want to delete this truck?" %></li>
</ul>
</div>

<div id="vehicleFooter">
Created: <%= @vehicle.created_at %> / Last Updated: <%= @vehicle.updated_at %>
</div>

