<% if logged_in? && !params.include?(:search) && !params.include?(:commit) %>
<%= render :partial => 'garage' %>
<% elsif params.include?(:search) %>
<%= render :partial => 'search' %>
<% else %>
<%= render :partial => 'results' %>
<% end %>
